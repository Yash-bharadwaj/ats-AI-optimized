# Core Framework - Absolute minimal
fastapi==0.110.1
uvicorn==0.25.0
pydantic==2.11.4

# HTTP client for API calls
httpx==0.28.1

# Vercel Serverless Support
mangum==0.17.0

# Optional file processing (will work without these)
PyPDF2==3.0.1
python-docx==1.2.0
python-multipart==0.0.20

# Removed for Vercel size limits:
# pymilvus - Use external API instead
# numpy - Not needed for basic operations
# pydantic-settings - Use os.getenv instead
# python-dotenv - Not needed for Vercel
# groq - Use direct HTTP calls instead 